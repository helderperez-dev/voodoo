# Voodoo examples

Examples are executable documentation. New examples are grouped by learning journey: `basics`, `web`, `data`, `ai`, `adaptive`, `distributed`, and `edge`. Existing examples are migrated only when their imports and documentation references can move together.
